{
  "name": "uon-webhook-receiver",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker receiving U-On.Travel CRM webhooks",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
