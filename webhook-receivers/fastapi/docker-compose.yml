services:
  receiver:
    build: .
    container_name: uon-webhook-receiver
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
